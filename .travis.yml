sudo: false
language: python
python:
  - "2.7"
before_install:
  - wget http://idolf.dk/travis_ci
install:
  - pip install --upgrade pip
script:
  - PWNLIB_NOTERM=1 coverage run -m sphinx -b doctest docs/source docs/build/doctest
