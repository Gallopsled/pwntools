from pwn import *
from rpyc.utils.registry import TCPRegistryClient

def cloud(code):
    hostinfo, _ = rpyc.discover('CLOUD', registrar = TCPRegistryClient('0.0.0.0'))
    ip, port = hostinfo
    c = rpyc.connect(ip, port)
    return c.root.doit(code)

